export default interface IShape {
  grid: Array<Array<boolean>>;
  color: string;
  // canGoLeft: (leftCells: Array<boolean>) => boolean;
  // canGoRight: (rightCells: Array<boolean>) => boolean;
  // canGoDown: (bottomCells: Array<boolean>) => boolean;
}